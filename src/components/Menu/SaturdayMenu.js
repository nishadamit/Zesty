import React from 'react';
import MenuList from './MenuList';
import MenuSidebar from './Sidebar';

function SaturdayMenu({dayListMenus}) {
  return (
    <MenuList  dayListMenus={dayListMenus} />
  )
}
export default SaturdayMenu;
